export class Todo {
    // id: number;
    // title: string;
    // isDone: boolean;
    // updatedAt: Date;

    constructor(public id: number, public title: string, public isDone: boolean, public updatedAt: Date) {
        // this.id = id;
        // this.title = title;
        // this.isDone = isDone;
        // this.updatedAt = updatedAt;        
    }
}
